sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl restart lamassu-admin-server
sudo supervisorctl restart lamassu-server

sudo supervisorctl status




# sudo supervisorctl stop lamassu-admin-server
# sudo supervisorctl stop lamassu-server
